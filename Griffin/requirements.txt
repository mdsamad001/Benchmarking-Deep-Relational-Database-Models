# ============================================================
# Griffin — Python dependencies
# ============================================================
# Install with:
#   pip install -r requirements.txt --break-system-packages
#
# PyTorch + CUDA must match your driver.  The versions below
# target CUDA 12.1; adjust the --index-url as needed.
# ============================================================

# ---- Core deep learning ----
torch>=2.1.0
torchvision>=0.16.0

# ---- PyG (PyTorch Geometric) ----
# Install AFTER torch so wheels match the CUDA version.
# If pip can't resolve, use:
#   pip install torch_geometric torch_scatter torch_sparse \
#       -f https://data.pyg.org/whl/torch-2.1.0+cu121.html
torch_geometric>=2.4.0
torch_scatter>=2.1.2

# ---- HuggingFace datasets ----
datasets>=2.14.0
pyarrow>=14.0.0

# ---- Accelerate (distributed training) ----
accelerate>=0.25.0

# ---- Metrics ----
torchmetrics>=1.2.0

# ---- Model I/O ----
safetensors>=0.4.0

# ---- Numerical / data ----
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0

# ---- Visualization (used by loss-curve plots + hmodel.py viz) ----
matplotlib>=3.7.0
seaborn>=0.12.0

# ---- Progress bars ----
tqdm>=4.65.0

# ---- Parallel data processing (dataconverter.py only) ----
pqdm>=0.2.0

# ---- Text embeddings (make_textemb.py only — optional) ----
# sentence-transformers>=2.2.0
