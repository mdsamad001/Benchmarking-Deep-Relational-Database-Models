# ============================================================
# DBFormer + RelBench — requirements.txt
# Verified on: DGX Spark GB10 (aarch64, CUDA 13.0, Python 3.12)
#
# THE MACHINE ALREADY HAS (pre-installed, do NOT reinstall):
#   torch 2.12.0+cu130, numpy 2.4.4, pandas 3.0.2,
#   torch_geometric 2.7.0, pytorch_frame 0.2.5, relbench 1.1.0,
#   lightning 2.6.1, scipy 1.17.1, scikit-learn 1.6.1,
#   matplotlib 3.10.8, attrs 26.1.0, tqdm 4.67.3,
#   transformers 5.9.0, torchmetrics 1.9.0
#
# THIS FILE ONLY INSTALLS WHAT IS MISSING.
# Run:  pip install -r requirements.txt
#
# CRITICAL PACKAGE NAME NOTE:
#   pytorch_frame  (0.x)  — PyG relational ML framework → imports as torch_frame ✓
#   torch-frame    (1.x)  — mmengine/MMLAB framework    → DO NOT INSTALL ✗
# ============================================================

# --- The 4 packages not pre-installed on DGX Spark GB10 ---
sqlalchemy>=2.0.0
inflect>=6.0.0
unidecode>=1.3.6
simple-parsing>=0.1.5